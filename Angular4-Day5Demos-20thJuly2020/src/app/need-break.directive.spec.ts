import { NeedBreakDirective } from './need-break.directive';

describe('NeedBreakDirective', () => {
  it('should create an instance', () => {
    const directive = new NeedBreakDirective();
    expect(directive).toBeTruthy();
  });
});
